---
import { Button } from "@/components/ui/button";
---

<section class="relative mt-24 pb-12 md:py-32 overflow-hidden">
    <!-- Background gradient that will be visible -->
    <div class="absolute inset-0 bg-gradient-to-b from-white to-gray-200 z-0">
    </div>

    <div
        class="container relative z-10 mx-auto max-w-7xl px-2 md:px-8 lg:px-12 space-y-10 md:space-y-8 flex flex-col justify-center items-center text-center"
    >
        <!-- Text -->
        <h1 class="font-bold text-3xl md:text-5xl lg:text-6xl text-gray-800 leading-tight">
            Convierte tus <span class="text-primary">Conceptos</span> en Realidad
        </h1>

        <p class="font-medium text-lg md:text-xl text-gray-600 max-w-2xl mt-3 md:mt-4 leading-relaxed">
            Lleva tus Ideas a la Tercera Dimensión con nuestros servicios
        </p>

        <!-- Decorative element -->
        <div class="w-28 h-1 bg-primary rounded-full my-4 md:my-6"></div>

        <!-- Button -->
        <div class="flex flex-col md:flex-row gap-4 md:gap-6 justify-center  items-center mt-5 md:mt-8 w-1/2 md:w-auto">
            <Button variant="secondary" class="w-full md:w-auto p-4 px-6 md:p-5 md:px-8">
                <p class="text-sm md:text-base">Ver Productos</p>
            </Button>

            <Button variant="default" class="w-full md:w-auto p-4 px-6 md:p-5 md:px-8">
                <p class="text-base md:text-lg font-medium">Cotizar</p>
            </Button>
        </div>
    </div>
</section>
